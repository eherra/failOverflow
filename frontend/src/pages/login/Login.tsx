import LoginForm from "./LoginForm";
import PageProvider from "../PageProvider";

const Login = () => {
  return (
    <PageProvider>
      <LoginForm />
    </PageProvider>
  )
};

export default Login;